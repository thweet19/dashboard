# THWEET Admin Dashboard — 제품 요구사항 문서 (PRD)

> **버전** v1.0-final  
> **작성일** 2026-06-07  
> **상태** Confirmed

---

## 1. 개요

### 1.1 목적
THWEET 3개 지점의 POS 매출 데이터를 자동으로 수집·가공하여, 대표 및 직원이 언제 어디서나 모바일로 확인할 수 있는 통합 매출 대시보드.

### 1.2 핵심 사용 시나리오
| 시나리오 | 설명 |
|---------|------|
| 일일 확인 | 대표/직원이 폰으로 당일 매출 현황 확인 |
| 주간 리뷰 | 주 1회 회의에서 대시보드 화면 공유하며 데이터 기반 의사결정 |

---

## 2. 기술 스택

| 항목 | 내용 |
|------|------|
| 구현 방식 | **단일 HTML 파일** (서버리스, GitHub Pages 배포) |
| 데이터 파이프라인 | GitHub Actions → Google Drive API → `data/latest.json` |
| 차트 | Chart.js 4.4 |
| 폰트 | Inter (UI), JetBrains Mono (수치) |
| 모바일 | **PWA** — 폰 홈화면에 앱으로 설치 가능 |

---

## 3. 데이터 파이프라인

### 3.1 흐름
```
Google Drive (매장별 폴더)
    ↓ GitHub Actions (하루 2회: 오전 10시 / 오후 10시 KST)
scripts/fetch_from_drive.py
    ↓ 엑셀 파싱 + 데이터 처리
data/latest.json
    ↓ git commit + push
GitHub Pages → index.html이 JSON fetch
```

### 3.2 Google Drive 폴더 구조
```
쓰윗 매출 대시보드/
├── 쓰윗성수/     → 성수홀 POS 주차별 파일 누적
├── 성수1층/      → 성수1층 POS 주차별 파일 누적
└── 쓰윗종각/     → 종각 POS 주차별 파일 누적
```
- 매주 해당 주 데이터 엑셀 파일 업로드 → 폴더에 누적
- 스크립트가 폴더 내 **모든 파일을 읽어 합산** 처리
- 파일명 규칙 없음 (폴더 기준으로 자동 감지)

### 3.3 GitHub Actions 스케줄
- KST 오전 10:00 → UTC 01:00
- KST 오후 10:00 → UTC 13:00
- Cron: `0 1,13 * * *`

### 3.4 POS 엑셀 시트 구조
| 시트명 | 사용 데이터 |
|--------|------------|
| 결제 합계 | 날짜 / 총 결제금액 / 결제건수 |
| 상품 주문 합계 | 날짜 / 상품명 / 카테고리 / 판매건수 / 실판매금액 |
| 결제 상세내역 | 결제시각 / 결제금액 (시간대 분석용) |

---

## 4. 지점 정의

| 탭 | 표시명 | Drive 폴더 | 색상 |
|----|--------|-----------|------|
| 전체 | 전체 통합 | — | `#F59E0B` Amber |
| seongsu | 쓰윗성수 | 쓰윗성수/ | `#10B981` Green |
| seongsu1f | 성수1층 | 성수1층/ | `#6C63FF` Purple |
| jonggak | 쓰윗종각 | 쓰윗종각/ | `#F472B6` Pink |

---

## 5. 정보 구조 (IA)

```
THWEET Admin Dashboard
│
├── [전체 통합]  [쓰윗성수]  [성수1층]  [쓰윗종각]   ← GNB (지점 선택)
│
└── Sub Tabs (지점별 공통)
    ├── [누적 매출현황]
    ├── [카테고리 품목분석]
    └── [패턴분석]
```
총 4 지점 × 3 탭 = 12개 화면 (공통 템플릿 적용)

---

## 6. 화면별 상세 기획

### 6.1 탭① — 누적 매출현황

**① 지점 비교 카드** (전체 탭 전용)
- 3개 지점 나란히 표시: 누적매출 / 일평균 / 객단가 / 비중(%)
- 클릭 시 해당 지점 탭으로 이동

**② KPI 스트립 (5개 지표)**
| # | 지표 | 산출 방식 |
|---|------|----------|
| 1 | 총 매출 | 기간 내 합계 |
| 2 | 일평균 매출 | 총매출 ÷ 영업일수 |
| 3 | 평균 객단가 | 총매출 ÷ 총결제건수 |
| 4 | 총 결제건수 | 기간 내 합계 |
| 5 | 방문객 수치 | 결제건수와 동일 |

**③ 누적 매출 추이 (선형 차트)**
- X축: 날짜 (MM/DD)
- Y축: 누적 매출 (고정 눈금)
- 레드 점선: 목표 라인 (우측 상단 입력 필드)
- 전체 탭: 지점별 3개 선 추가 표시
- 하단: 목표 달성률 %

**④ 일별 매출 기록 테이블**
| 컬럼 | 내용 |
|------|------|
| 날짜 | YYYY-MM-DD (Mono) |
| 요일 | 태그 형태 |
| 매출 | X,XXX,XXX원 |
| 건수 | XX건 |
| 객단가 | XX,XXX원 |
| 비중 | 최고일 대비 비율 바 |

---

### 6.2 탭② — 카테고리 품목분석

**카테고리 정의**
| 카테고리 | 색상 |
|---------|------|
| 위스키 아이스크림 | `#F59E0B` Amber |
| 논알콜 젤라또 | `#6C63FF` Purple |
| 커피 | `#10B981` Green |
| 에이드 | `#34D399` Teal |
| 디저트 | `#F472B6` Pink |
| 위스키 | `#38BDF8` Blue |

**구성 요소**
- 도넛 차트: 카테고리별 매출 비중
- 가로 바 차트: 카테고리별 판매건수 분포
- Top 3 / Bottom 3: 카테고리 탭 선택 시 해당 카테고리 내 메뉴 순위

---

### 6.3 탭③ — 패턴분석

- 요일별 평균 매출 (세로 바 차트, 7개 컬럼)
- 시간대별 누적 매출 (세로 바 차트, 11시~22시)
- 시간대별 결제건수 (세로 바 차트, 비교용 2컬럼 레이아웃)

---

## 7. 목표 매출 (분기별 고정, 코드에 하드코딩)

| 지점 | 기본 목표 |
|------|---------|
| 전체 통합 | 5,000만원 |
| 쓰윗성수 | 2,000만원 |
| 성수1층 | 1,500만원 |
| 쓰윗종각 | 1,200만원 |

> 분기 변경 시 `index.html` 내 `TARGETS` 상수 수정

---

## 8. 인증

- 비밀번호 단일 로그인 (내부 공유)
- GitHub Secrets에 저장: `DASHBOARD_PASSWORD`
- 로컬 개발 기본값: `thweet2024` (실제 비밀번호는 Secret으로 관리)

---

## 9. 디자인 시스템

### 9.1 컬러 팔레트 (다크 테마)
| 토큰 | 값 | 용도 |
|------|----|------|
| `--bg` | `#0A0A0F` | 페이지 배경 |
| `--surface` | `#111118` | 카드, 사이드바 |
| `--surface2` | `#18181F` | 호버 배경 |
| `--border` | `#2A2A38` | 기본 테두리 |
| `--text` | `#E8E8F0` | 주 텍스트 |
| `--text2` | `#9090A8` | 보조 텍스트 |
| `--accent` | `#6C63FF` | 강조, 활성 상태 |

### 9.2 반응형
| 브레이크포인트 | 처리 |
|--------------|------|
| ≤ 900px (모바일) | 사이드바 숨김, KPI 2컬럼, 차트 1컬럼 |
| > 900px | 풀 레이아웃 |

---

## 10. 파일 구조

```
Thweet Dashboard/
├── index.html                        ← 대시보드 (단일 HTML)
├── manifest.json                     ← PWA 매니페스트
├── icons/                            ← PWA 아이콘
├── data/
│   └── latest.json                   ← GitHub Actions가 생성
├── scripts/
│   └── fetch_from_drive.py           ← Drive 다운로드 + 처리
├── .github/
│   └── workflows/
│       └── update.yml                ← 하루 2회 자동 실행
├── requirements.txt
└── .gitignore
```

---

## 11. GitHub Secrets (설정 필요)

| Secret 명 | 내용 |
|----------|------|
| `GOOGLE_SERVICE_ACCOUNT_JSON` | Google 서비스 계정 키 (JSON 전체) |
| `GOOGLE_DRIVE_FOLDER_ID` | 쓰윗 매출 대시보드 상위 폴더 ID |
| `DASHBOARD_PASSWORD` | 대시보드 로그인 비밀번호 |

---

## 12. 로드맵

| 버전 | 기능 |
|------|------|
| **v1.0** | 현재 스펙 전체 구현 |
| v1.1 | 기간 필터 (이번달 / 3개월 / 전체) |
| v1.2 | 세트 구매율 지표 |
| v2.0 | 원가 입력 → 공헌이익 분석 |
| v2.1 | 이벤트 태깅 (Mr.위스키 콜라보 등) |
| v2.2 | 일 매출 목표 미달 시 알림 |
